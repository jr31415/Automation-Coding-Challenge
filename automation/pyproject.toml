[project]
name = "automation"
version = "0.1.0"
description = "Computer-use automation system: LLM-driven discovery, deterministic replay, and human escalation for legacy back-office UIs."
readme = "README.md"
authors = [
    { name = "jr31415", email = "jacob@rosen4.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40.0",
    "playwright>=1.48.0",
    "pydantic>=2.9.0",
    "python-dotenv>=1.2.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
]

[project.scripts]
discover = "automation.cli.discover:main"
replay = "automation.cli.replay:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.pytest.ini_options]
# uv's editable-install .pth file has been observed to get silently
# truncated (missing trailing newline) on paths containing spaces, which
# drops "automation" off sys.path. Setting pythonpath explicitly here makes
# `uv run pytest` work regardless of that install detail.
pythonpath = ["src"]
